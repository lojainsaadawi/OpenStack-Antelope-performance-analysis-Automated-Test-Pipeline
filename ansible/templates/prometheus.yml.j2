global:
  scrape_interval: {{ scrape_interval }}

scrape_configs:
  - job_name: 'openstack_nodes'
    static_configs:
      - targets:
{% for node, ip in openstack_nodes.items() %}
        - '{{ ip }}:{{ node_exporter_port }}'
{% endfor %}
